extends ./_shared/layout
block title
	title Workers
block content	
	h1 Workers
	
	ul
		each worker in workers
			li(id= worker.id).worker
				span= worker.id
				span= worker.name
				span= worker.gender